apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "form-builder-chart.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "form-builder-chart.name" . }}
    helm.sh/chart: {{ include "form-builder-chart.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "form-builder-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
